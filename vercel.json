{
  "version": 2,
  "functions": {
    "api/wsgi.py": { "runtime": "python3.12" }
  },
  "installCommand": "python -m pip install --upgrade pip && python -m pip install -r requirements.txt",
  "buildCommand": "if [ -f manage.py ]; then DJANGO_SETTINGS_MODULE=${DJANGO_SETTINGS_MODULE:-blog.settings} python manage.py collectstatic --noinput; fi",
  "routes": [
    { "src": "/static/(.*)", "dest": "/staticfiles/$1" },
    { "src": "/(.*)", "dest": "api/wsgi.py" }
  ]
}
