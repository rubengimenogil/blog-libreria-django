{
  "version": 2,
  "functions": {
    "api/wsgi.py": { "runtime": "python3.12" }
  },
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "python manage.py collectstatic --noinput",
  "routes": [
    { "src": "/static/(.*)", "dest": "/staticfiles/$1" },
    { "src": "/(.*)", "dest": "api/wsgi.py" }
  ]
}
