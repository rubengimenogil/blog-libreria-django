{
  "version": 2,
  "builds": [
    { "src": "api/index.py", "use": "@vercel/python" }
  ],
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "if [ -f manage.py ]; then python manage.py collectstatic --noinput; elif [ -f ../manage.py ]; then python ../manage.py collectstatic --noinput; else echo 'manage.py no encontrado, omitiendo collectstatic'; fi",
  "routes": [
    { "src": "/static/(.*)", "dest": "staticfiles/$1" },
    { "src": "/(.*)", "dest": "api/index.py" }
  ]
}
